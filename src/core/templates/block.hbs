{{! make the _globals object in course.json available to this template}}
{{import_globals}}
<div class="block-inner">

    {{#if displayTitle}}
        <div class="block-title">
            {{#unless _disableAccessibilityState}}
                <div class="js-heading"></div>
            {{/unless}}
            <div class="block-title-inner" {{#unless _disableAccessibilityState}}aria-hidden="true"{{/unless}}>
                {{{compile displayTitle this}}}
            </div>
        </div>
    {{/if}}

    {{#if body}}
        <div class="block-body">
            <div class="block-body-inner">
                {{{compile_a11y_text body this}}}
            </div>
        </div>
    {{/if}}

    {{#if instruction}}
        <div class="block-instruction">
            <div class="block-instruction-inner">
                {{{compile_a11y_text instruction this}}}
            </div>
        </div>
    {{/if}}

    <div class="component-container">
    </div>
</div>